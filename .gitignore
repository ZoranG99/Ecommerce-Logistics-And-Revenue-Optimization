# 1. Ignore all CSV and ZIP files everywhere
**/*.csv
**/*.zip

# 2. Allow the data folder itself to be tracked
!data/
!data/raw/
!data/processed/

# 3. Ignore everything INSIDE those folders
data/raw/*
data/processed/*

# 4. Do NOT ignore the .gitkeep files (this keeps the folders "non-empty")
!.gitkeep